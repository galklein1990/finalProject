extends layout

block content
    -data = data || {}
    -registered = registered || false 
    if registered
        div Upload Images
        br
        <form action="." method="post">Select images:
            <input type="file" name="img" multiple>
             <input type="submit">    

        //form(method="post" enctype="multipart/form-data" action="/upload")
            input( type="file" webkitdirectory mozdirectory)
            
    else
        a(
            class='JoinNow'
            href = '/join'
        )   Join now 
        br
        a(
        class='signIn'
        href = '/signIn'
        ) Sign in 
        br 